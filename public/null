




Teacher::with('dataTeacher', 'teacherSubject', 'teacherGrade', 'teacherExtracurricular', 'userable', 'project', 'projectCoordinator')->find(1)





Teacher::with('dataTeacher', 'teacherSubject', 'teacherGrade', 'teacherExtracurricular', 'userable', 'project', 'projectCoordinator')->find(1)
Teacher::all()->each(function ($teacher) {
    $teacher->ulid = Str::ulid()->toBase32();
    $teacher->save();
});









Student::all()->each(function ($student) {
    $student->ulid = Str::ulid()->toBase32();
    $student->save();
});







Student::all()->each(function ($student) {
    $student->ulid = Str::ulid()->toBase32();
    $student->save();
});







Student::all()->each(function ($student) {
    $student->ulid = Str::ulid()->toBase32();
    $student->save();
});







Student::all()->each(function ($student) {
    $student->ulid = Str::ulid()->toBase32();
    $student->save();
});







Student::all()->each(function ($student) {
    $student->ulid = Str::ulid()->toBase32();
    $student->save();
});







Student::all()->each(function ($student) {
    $student->ulid = Str::ulid()->toBase32();
    $student->save();
});







Student::all()->each(function ($student) {
    $student->ulid = Str::ulid()->toBase32();
    $student->save();
});







Student::all()->each(function ($student) {
    $student->ulid = Str::ulid()->toBase32();
    $student->save();
});







Grade::all()->each(function ($grade) {
    $grade->ulid = Str::ulid()->toBase32();
    $grade->save();
});
